{
  "app.error-boundary": "No test throws inside a page to see the error screen; a component test (npm run test:ui) can now",
  "app.updates.desktop": "The Tauri updater needs a signed release feed; the Desktop harness only has a boot smoke test",
  "settings.storage-used": "Nothing asserts the storage figure in Settings; a component test (npm run test:ui) can",
  "storage.local": "The local file storage adapter (WISP 1001) is exercised only through backup.profile.file, never on its own",
  "core.dht-direct": "src-tauri/src/pkarr_client.rs has no Rust test, and the Desktop smoke test only reads the label in Settings",
  "wallet.cashu.receive-token": "Receiving a pasted token from the wallet page is untested; the chat token card (payments.cashu.token-card) is",
  "wallet.cashu.export": "walletExport has no test at all",
  "proofs.oidc.callback.extension": "The chromiumapp.org redirect needs a real extension identity flow; no extension OIDC spec exists",
  "desktop.bundle-wiring": "Guarded by npm run check:desktop-bundle in PR CI, a build assertion the test map does not read",
  "desktop.notifications": "Native notifications need the Desktop harness (Linux only) and no spec drives them",
  "desktop.local-fetch": "Rust local_fetch has no test; services are only e2e-tested in the extension",
  "desktop.crypto": "The Rust crypto commands have no direct test; parity is only indirect through core's interop-rust suite",
  "cli.identity": "cli/ has no tests at all",
  "cli.invite": "cli/ has no tests at all",
  "cli.send": "cli/ has no tests at all; only the gated interop-rust suite drives the binary",
  "cli.recv": "cli/ has no tests at all; only the gated interop-rust suite drives the binary"
}
