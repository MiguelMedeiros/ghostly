# Screenshots for PR #258 (website: the scroll film back on upright windows)

- `clips/<size>-<speed>.{gif,mp4}`: the first act (hero, invitation, meeting place) side by side in four builds:
  before #202 (80fc7bd0), before #241 (2ad103eb), dev (5bfb5ca1) and PR #258. Recorded headlessly with
  `website/scripts/motion/speeds.mjs` and stacked with `stack.sh`, lined up on the first scroll.
  Speeds: slow (0.8 viewport heights a second), flick (four 2.2-viewport trackpad flings), reverse (four flings up).
- `stills/`: the film at upright and landscape sizes, the ghosts' hem before and after, the connect burst, the
  nav and footer before and after, and the app's own wordmark for reference.
