# Developers: the protocol explainer (PR #259)

Evidence for https://github.com/MiguelMedeiros/ghostly/pull/259.

- `steps/`: every step's finished frame at 1440×900 (desktop, copy beside the stage), 1009×1239 (the Claude app's upright browser panel) and 390×844 (phone, portrait stage), with reduced motion so each is the step's still.
- `pt-br/`: the same in Portuguese at 1440×900 and 390×844.
- `video/`: stepping forward through all eight steps, back twice (each plays in reverse), Replay step, and a jump by dot, recorded headless at each size (GIF and MP4).
