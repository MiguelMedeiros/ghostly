/**
 * @ghostly/sdk/core: the whole protocol library (`packages/core`), for a client that implements only
 * the WISP contracts it needs: identities and records, invites, Pkarr rendezvous, the data link and
 * its frames, paired sessions and their capabilities, payments, identity proofs. It follows the WISP
 * drafts it implements: anything here may change with them, minor by minor, until they are Final.
 */
export * from "@ghostly/core";
