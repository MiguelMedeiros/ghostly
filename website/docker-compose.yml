services:
  website:
    build:
      # The repository root: the build reads docs/ and packages/core/src
      # (Dockerfile.dockerignore keeps the context to those).
      context: ..
      dockerfile: website/Dockerfile
    ports:
      - "${WEBSITE_PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
